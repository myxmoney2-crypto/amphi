# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-anon-key
# Service role key — server only, never exposed to the browser. Used to
# reset course status and as a fallback admin client from the trigger route.
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key

# Note : OPENAI_API_KEY / ANTHROPIC_API_KEY ne sont PAS nécessaires ici — le
# traitement (Whisper + Claude) tourne dans l'Edge Function Supabase
# `process-course`, configurée séparément via `supabase secrets set`.

# Resend — transactional emails (optional for local dev)
RESEND_API_KEY=re_xxxxxxxx
